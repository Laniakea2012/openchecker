apiVersion: apps/v1
kind: Deployment
metadata:
  name: openchecker-test-agent
  namespace: openchecker
  labels:
    app: openchecker-test-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openchecker-test-agent
  template:
    metadata:
      labels:
        app: openchecker-test-agent
    spec:
      containers:
      - name: openchecker-test-agent
        image: guoqiangqi/openchecker:v0.22
        imagePullPolicy: Always
        command: ["python", "-u", "openchecker/agent.py"]

        volumeMounts:
        - name: config-volume
          mountPath: /app/config/
      volumes:
      - name: config-volume
        persistentVolumeClaim:
          claimName: openchecker-test-pvc